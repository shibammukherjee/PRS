contains 10M dataset reduced-dataset (recommendation model) with its stash, cluster, group datasets


please add the full dataset in ratings.csv

generate the recommendation model in main.cpp by changing the parameters

then generate the stash, cluster, group sets

there is config.cpp which one might need to tune for better performance and correct settings

then run the PRS :)